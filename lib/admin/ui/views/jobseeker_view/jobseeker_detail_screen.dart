import 'package:flutter/material.dart';

class JobseekerDetailScreen extends StatelessWidget {
  const JobseekerDetailScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Hiển thị thông tin ứng viên'),
      ],
    );
  }
}
